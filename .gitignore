# ---------------------------------------------------------------
# Enhets-/förbandsspecifikt — distribueras separat, aldrig i git
# ---------------------------------------------------------------
# Serverpaket med TAK-serveradress och certifikat. Varje förband
# lägger in sin egen fil med exakt namnet atak-box.zip.
atak-box.zip

# Certifikat, nycklar och VPN-profiler. OpenVPN ingar inte langre i
# paketet, men regeln star kvar som skyddsnat: en .ovpn-fil ar en
# hemlighet oavsett vilken klient den hor till.
*.p12
*.pfx
*.jks
*.key
*.pem
*.crt
*.cer
*.ovpn

# ---------------------------------------------------------------
# Foljer med nar atak/-mappen kopieras fran en konfigurerad telefon,
# men hor inte hemma i repot.
# ---------------------------------------------------------------
# ATAK lagger sjalv ut sin dokumentation ur appens assets vid varje
# uppgradering (AppVersionUpgrade), sa kopior i payload skrivs anda over.
**/atak/support/docs/*.pdf

# Plugin-apk ur ett redan importerat datapaket. Plugin-apparna hamtas ur
# Play Store; ingenting i paketet installerar dem harifran.
**/atak/tools/datapackage/files/**/*.apk

# ---------------------------------------------------------------
# ATAK-CIV och plugin-apparna sidladdas, men hamtas av forbandet
# sjalvt - de ar tredje parts binarer och ligger inte i repot. Samma
# princip som atak-box.zip. Ramsor och HVreports ar Hemvarnets egna
# och foljer med.
#
# Regeln ar medvetet omvand: allt under payload/apks/ ar utestangt och
# de tva egna apparna slapps in igen. En namnprefixregel hade slappt
# igenom varje ny leverantorsfil vars namn inte borjar pa ATAK-, och
# filnamnen bestams inte av oss - tak.gov kallar t.ex. GeoCam-appen
# camera-<version>-<hash>-release.apk. Verktyget letar anda upp
# apk-filerna pa paket-id, inte pa filnamn, sa reglerna har paverkar
# bara vad som hamnar i git.
# ---------------------------------------------------------------
payload/apks/*
!payload/apks/Ramsor.apk
!payload/apks/HVreports.apk
!payload/apks/ATAK-Plugin-hvreports-*.apk
*.apks

# ---------------------------------------------------------------
# Tredjepartsbinärer — hämtas från leverantören, se README
# ---------------------------------------------------------------
# Android SDK Platform-Tools. Bara det adb behover foljer med (Windows):
# adb.exe + DLL:er, licens och versionsinfo. fastboot, sqlite3, mke2fs
# m.fl. anvands inte av paketet och halls utanfor - fastboot kan dessutom
# radera en telefon om nagon kor fel verktyg.
platform-tools/*
!platform-tools/adb.exe
!platform-tools/AdbWinApi.dll
!platform-tools/AdbWinUsbApi.dll
!platform-tools/source.properties

# ---------------------------------------------------------------
# Leverantorsdokumentation - laddas ner, distribueras inte vidare
# ---------------------------------------------------------------
# TAK Product Centers egna dokument (serverguide, installationsskript,
# utbildningsdeckar) ligger bakom inloggning pa tak.gov. Repot ar
# publikt, sa de hamtas dar av den som behover dem i stallet for att
# spridas harifran. Samma princip som apk-filerna.
TakServerDocs/

# ---------------------------------------------------------------
# För stort för git
# ---------------------------------------------------------------
# Originaldokument (.pptx/.docx/.pdf) som konverterats till Markdown
_source/

# ---------------------------------------------------------------
# Loggar och skräp
# ---------------------------------------------------------------
*.log
logs/

# Python
__pycache__/
*.py[cod]
Thumbs.db
desktop.ini
.DS_Store
